sequenceDiagram
    participant browser
    participant server

    browser->>server: main.js : form.onsubmit() is called after submitting form
    activate server
    server-->>browser: Push a notes object locally to list of notes & Redraw notes
    deactivate server
    Note right of browser: Only locally append the changes after form submission

    browser->>server: main.js : sendToServer() - POST Request to server with new note
    activate server
    server-->>browser: HTTP 301 : note updated at server and JSON response received
    deactivate server
    Note right of browser: Send a post request to the server to update notes, but we don't reload since local data was just updated
    browser<<->>server: Also, webpage does not redraw server notes again until reload, but shows the locally updated data instance
