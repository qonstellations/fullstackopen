sequenceDiagram
    participant browser
    participant server

    browser->>server: GET https://studies.cs.helsinki.fi/exampleapp/spa
    activate server
    server-->>browser: HTTP 200 : Returns HTML document
    deactivate server

    Note right of browser: This request fetches the main HTML document for the webpage

    browser->>server: GET https://studies.cs.helsinki.fi/exampleapp/main.css
    activate server
    server-->>browser: HTTP 200 : Returns main.css file from server
    deactivate server

    Note right of browser: Fetches CSS stylesheet for the page from the href given in the HTML header

    browser->>server: GET https://studies.cs.helsinki.fi/exampleapp/spa.js
    activate server
    server-->>browser: HTTP 200 : Returns main.js file
    deactivate server

    Note right of browser: Fetches JS file from server after script tag invokes it
    
    browser->>server: GET https://studies.cs.helsinki.fi/exampleapp/data.json
    activate server
    server-->>browser: HTTP 200 : Returns a list of json-ified notes
    deactivate server
    Note right of browser: Sends an XHTTP GET request for the data.json file from main.js file

    browser->>server: main.js : onreadystatechange() called after xhttp.send()
    activate server
    server-->>browser: If previous GET request received with HTTP 200, Parse JSON Response & redraw Notes
    deactivate server
    Note right of browser: Sends an XHTTP GET request for the data.json file from main.js file
